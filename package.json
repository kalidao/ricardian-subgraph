{
  "name": "ricardian",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ nerderlyne/ricardian",
    "create-local": "graph create --node http://localhost:8020/ nerderlyne/ricardian",
    "remove-local": "graph remove --node http://localhost:8020/ nerderlyne/ricardian",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nerderlyne/ricardian"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
